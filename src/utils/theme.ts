import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#31493C',
    },
    secondary: {
      main: '#50A2A7',
    },
    background: {
      paper: '#fff',
      default: '#fff',
    },
  },
});
